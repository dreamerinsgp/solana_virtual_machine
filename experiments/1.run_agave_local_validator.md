Step1: Add logs 
info!()


Step2: rebuild the validator 
   ./cargo build --release --bin solana-test-validator


Step3: Restart the validator 
     pkill -f solana-test-validator
   LOG_FILE=validator.log ./start-agave-node.sh
  

Step4: Send a transaction and check logs 
  A transfer transaction :
    1) solana-keygen new --no-bip39-passphrase --outfile /tmp/test.json --force
    2) DEST=$(solana-keygen pubkey /tmp/test.json)
    3) solana transfer $DEST 0.1 --allow-unfunded-recipient